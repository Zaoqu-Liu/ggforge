% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxviolinplot.R
\name{validate_paired_groups}
\alias{validate_paired_groups}
\title{Validate paired data with groups}
\usage{
validate_paired_groups(data, x, paired_by, group_by)
}
\arguments{
\item{data}{Data frame to validate}

\item{x}{Character string; x-axis column name}

\item{paired_by}{Character string; pairing column name}

\item{group_by}{Character string; grouping column name}
}
\value{
NULL (invisibly); throws error if validation fails
}
\description{
Validates that paired data with grouping has the correct structure:
each combination of x and paired_by should have exactly two observations,
one for each group.
}
\examples{
\dontrun{
# Valid paired data with groups
df <- data.frame(
  time = rep(c("Before", "After"), each = 6),
  subject = rep(1:6, 2),
  group = rep(c("Control", "Treatment"), 6),
  value = rnorm(12)
)
validate_paired_groups(df, "time", "subject", "group")

# Invalid data will throw an error
df_invalid <- df[-1, ] # Remove one observation
# validate_paired_groups(df_invalid, "time", "subject", "group")
# Error: validation fails
}

}
\keyword{internal}
