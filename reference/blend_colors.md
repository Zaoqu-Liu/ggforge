# Blend multiple colors

Blend multiple colors

## Usage

``` r
blend_colors(colors, mode = c("blend", "average", "screen", "multiply"))
```

## Arguments

- colors:

  Vector of colors

- mode:

  Blend mode

## Value

Blended color
