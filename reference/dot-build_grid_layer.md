# Build grid velocity layer

Build grid velocity layer

## Usage

``` r
.build_grid_layer(
  embedding,
  v_embedding,
  density,
  smooth,
  n_neighbors,
  min_mass,
  scale,
  arrow_angle,
  arrow_color,
  arrow_alpha
)
```
