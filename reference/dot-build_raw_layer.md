# Build raw velocity layer

Build raw velocity layer

## Usage

``` r
.build_raw_layer(
  embedding,
  v_embedding,
  density,
  scale,
  group_by,
  group_name,
  group_palette,
  group_palcolor,
  arrow_angle,
  arrow_color,
  arrow_alpha
)
```
