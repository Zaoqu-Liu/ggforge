# Merge plot parameters

Intelligently merge user-provided parameters with defaults

## Usage

``` r
merge_params(user_params, defaults)
```

## Arguments

- user_params:

  User-provided parameters

- defaults:

  Default parameters

## Value

Merged parameter list
