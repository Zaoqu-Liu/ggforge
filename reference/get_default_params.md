# Get default plot parameters

Returns a list of default parameters for all plot types

## Usage

``` r
get_default_params(plot_type = NULL)
```

## Arguments

- plot_type:

  Optional plot type to get specific defaults

## Value

List of default parameters
