# Validate choice from options

Validate choice from options

## Usage

``` r
validate_choice(value, param_name, choices, allow_null = TRUE)
```

## Arguments

- value:

  Value to validate

- param_name:

  Parameter name

- choices:

  Valid choices

- allow_null:

  Whether NULL is allowed

## Value

The validated value
